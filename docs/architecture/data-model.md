# Data Model

> This document is a stub. Will expand the graph schema from `ARCHITECTURE.md` with full Kuzu DDL, property types, constraints, and index definitions.

## Placeholder

Content will cover:

- Full node definitions with property types
- Full edge definitions with cardinality
- Kuzu schema DDL (CREATE NODE TABLE, CREATE REL TABLE)
- Index and full-text search configuration
- Derived edge computation (SIMILAR_TO, RELATED_TO)
- Migration strategy
