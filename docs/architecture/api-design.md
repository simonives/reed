# API Design

> This document is a stub. Will specify the full REST API surface, request/response schemas, error handling conventions, and pagination.

## Placeholder

Content will cover:

- Base URL and versioning strategy
- Authentication (`X-API-Key` header)
- Resource groups: `/feeds`, `/items`, `/tags`, `/topics`, `/search`, `/graph`
- Request and response schemas (Pydantic models)
- Error response format
- Pagination
- Rate limiting (if any)
- OpenAPI spec location (`/docs`)
